# SQLite migration archive

Preserved when switching Prisma datasource provider from `sqlite` to `postgresql`
(Supabase). These migrations are historical only and must not be applied to Postgres.

Active migrations live in `../migrations/` with `provider = "postgresql"`.
